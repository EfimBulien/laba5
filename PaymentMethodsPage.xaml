<Page x:Class="laba5.PaymentMethodsPage"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:laba5"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PaymentMethodsPage">
    <Grid>
        <StackPanel VerticalAlignment="Bottom">
            <TextBlock Text="Добавить способ оплаты:" 
                       Margin="5" 
                       FontWeight="Bold"
                       Foreground="Gray"/>
            <TextBox x:Name="PaymentMethodBox" 
                     materialDesign:HintAssist.Hint="Способ оплаты" 
                     Margin="5"/>
            <Button x:Name="AddButton" 
                    Content="Добавить способ оплаты" 
                    Click="AddButton_Click" 
                    Margin="5"/>

            <TextBlock Text="Редактировать/Удалить способ оплаты:"
                       Margin="5" 
                       FontWeight="Bold" 
                       Foreground="Gray"/>
            <TextBox x:Name="IDbox" 
                     materialDesign:HintAssist.Hint="ID способа оплаты" 
                     Margin="5"/>
            <TextBox x:Name="EditPaymentMethodBox" 
                     materialDesign:HintAssist.Hint="Новый способ оплаты" 
                     Margin="5"/>
            <Button x:Name="EditButton" 
                    Content="Изменить" 
                    Click="EditButton_Click" 
                    Margin="5"/>
            <Button x:Name="DelButton" 
                    Content="Удалить" 
                    Click="DelButton_Click" 
                    Margin="5"/>
        </StackPanel>
    </Grid>
</Page>