<Page x:Class="laba5.RegPage"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:laba5"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="RegPage">

    <Grid>
        <StackPanel VerticalAlignment="Bottom">
            <TextBox x:Name="LoginBox" materialDesign:HintAssist.Hint="Логин"/>
            <TextBox x:Name="PasswordBox" materialDesign:HintAssist.Hint="Пароль"/>
            <TextBox x:Name="IDBox" materialDesign:HintAssist.Hint="ID"/>
            <Button x:Name="RegButton" Content="Зарегестрировать" VerticalAlignment="Bottom" Click="RegButton_Click"/>
            <Button x:Name="EditButton" Content="Обновить" VerticalAlignment="Bottom" Click="EditButton_Click"/>
            <Button x:Name="DeleteButton" Content="Удалить" VerticalAlignment="Bottom" Click="DeleteButton_Click"/>
        </StackPanel>
    </Grid>
</Page>
